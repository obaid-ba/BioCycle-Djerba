# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ---- Database ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# ---- Config & validation ----
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- Auth (used from Phase 1 onward) ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin <4.1: passlib 1.7.4 reads bcrypt's version via __about__, removed in 4.1+
# (harmless "error reading bcrypt version" warning otherwise).
bcrypt==4.0.1

# ---- Realtime / messaging (used from Phase 4 onward) ----
paho-mqtt==2.1.0

# ---- HTTP client for AI integration (Phase 5) ----
httpx==0.28.1

# ---- Firebase Realtime DB (read-only): service-account -> OAuth token, read
# via REST with httpx. google-auth is light vs. the full firebase-admin SDK.
# `requests` is used by google-auth's token transport for the refresh call. ----
google-auth==2.35.0
requests==2.32.3

# ---- Utils ----
python-multipart==0.0.20
