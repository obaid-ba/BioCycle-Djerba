"""Reports: period-scoped summaries and CSV exports of collection requests.

Distinct from analytics (which powers the live dashboard): reports are built for
a chosen date range and meant to be downloaded/printed. Self-contained so it
doesn't perturb the dashboard's queries.
"""
